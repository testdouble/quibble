{
  "name": "quibble-example",
  "private": true,
  "scripts": {
    "test": "node --loader=quibble --test",
    "test-auto-loader": "node --test"
  },
  "devDependencies": {
    "quibble": "file:.."
  }
}
