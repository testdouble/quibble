{
  "name": "quibble-example",
  "private": true,
  "scripts": {
    "test": "mocha --loader=quibble --experimental-json-modules test/helper.js --recursive test/lib/"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^9.1.3",
    "quibble": "file:.."
  }
}
