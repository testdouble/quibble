{
  "name": "quibble-example",
  "private": true,
  "scripts": {
    "test": "mocha --loader=quibble test/helper.js --recursive test/lib/"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^10.1.0",
    "quibble": "file:.."
  }
}
